echo_mpclient + select function

멀티프로세스 클라이언트를 수정. (select를 사용)
=> 셀렉트하고 변화가 일어났으면...(사용자 입력, 서버로부터 메시지(클라이언트 소켓).
for문 필요 x. 위에 2개만 확인하면된다. 
사용자로부터 입력받으면 write루틴
서버로부터 메시지..받으면 read루틴.
write, read루틴은.
while을돌면서 읽고....그것을 출력 -> 이것은 문제가된다.
while을 지워줘야한다.

